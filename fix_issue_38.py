```json
{
  "solution_code": "### File 1: build_llama_metal.sh\n\n```bash\n#!/usr/bin/env bash\n# build_llama_metal.sh - Patched llama.cpp build with StorageModeManaged fix\n# for discrete AMD GPUs on Mac Pro 2013, iMac 2014-2019, MacBook Pro 2015-2019\n#\n# Reference: https://github.com/ggml-org/llama.cpp/pull/20615\n\nset -euo pipefail\n\nLLAMA_REPO=\"https://github.com/ggml-org/llama.cpp.git\"\nBUILD_DIR=\"${HOME}/.trashclaw/llama_build\"\nINSTALL_DIR=\"${HOME}/.trashclaw/llama_bin\"\nLLAMA_TAG=\"${LLAMA_TAG:-b3447}\"  # last known good tag; override with env var\n\nprint_info()  { echo \"[INFO]  $*\"; }\nprint_ok()    { echo \"[OK]    $*\"; }\nprint_warn()  { echo \"[WARN]  $*\"; }\nprint_err()   { echo \"[ERROR] $*\" >&2; }\n\n# ── prerequisites ──────────────────────────────────────────────────────────────\ncheck_prerequisites() {\n    print_info \"Checking prerequisites...\"\n    local missing=()\n    for cmd in git cmake clang; do\n        command -v \"$cmd\" &>/dev/null || missing+=(\"$cmd\")\n    done\n    if [[ ${#missing[@]} -gt 0 ]]; then\n        print_err \"Missing tools: ${missing[*]}\"\n        print_err \"Install Xcode Command Line Tools:  xcode-select --install\"\n        exit 1\n    fi\n    # Metal / Xcode frameworks\n    if [[ ! -d \"/System/Library/Frameworks/Metal.framework\" ]]; then\n        print_err \"Metal.framework not found. macOS 10.13+ required.\"\n        exit 1\n    fi\n    print_ok \"Prerequisites satisfied.\"\n}\n\n# ── clone / update ─────────────────────────────────────────────────────────────\nfetch_llama_cpp() {\n    mkdir -p \"$BUILD_DIR\"\n    if [[ -d \"$BUILD_DIR/llama.cpp/.git\" ]]; then\n        print_info \"Updating existing llama.cpp clone...\"\n        git -C \"$BUILD_DIR/llama.cpp\" fetch --tags --quiet\n    else\n        print_info \"Cloning llama.cpp...\"\n        git clone --depth=1 --branch \"$LLAMA_TAG\" \"$LLAMA_REPO\" \"$BUILD_DIR/llama.cpp\" 2>&1 | tail -5\n    fi\n    git -C \"$BUILD_DIR/llama.cpp\" checkout \"$LLAMA_TAG\" --quiet\n    print_ok \"Source at tag $LLAMA_TAG.\"\n}\n\n# ── the 3-line StorageModeManaged patch ────────────────────────────────────────\n# Original upstream code allocates Metal buffers with MTLStorageModeShared,\n# which on discrete (non-UMA) AMD GPUs causes GPU-invisible allocations.\n# The fix switches to MTLStorageModeManaged so the driver synchronises\n# between system RAM and VRAM correctly.\n#\n# File: ggml/src/ggml-metal/ggml-metal.m  (path may vary by version)\napply_storage_mode_patch() {\n    local src\n    # The exact file path has moved between versions; try both locations.\n    for candidate in \\\n        \"$BUILD_DIR/llama.cpp/ggml/src/ggml-metal/ggml-metal.m\" \\\n        \"$BUILD_DIR/llama.cpp/ggml/src/ggml-metal.m\" \\\n        \"$BUILD_DIR/llama.cpp/src/llama-metal.m\"\n    do\n        [[ -f \"$candidate\" ]] && { src=\"$candidate\"; break; }\n    done\n\n    if [[ -z \"${src:-}\" ]]; then\n        print_err \"Cannot find ggml-metal.m in the llama.cpp source tree.\"\n        print_err \"Check BUILD_DIR=$BUILD_DIR manually.\"\n        exit 1\n    fi\n    print_info \"Applying StorageModeManaged patch to: $src\"\n\n    # Guard: skip if already patched\n    if grep -q 'StorageModeManaged' \"$src\"; then\n        print_ok \"Patch already applied — skipping.\"\n        return\n    fi\n\n    # The patch:\n    #   1. Add a helper that returns Managed on discrete GPU, Shared on UMA.\n    #   2. Replace hard-coded MTLStorageModeShared with the helper call.\n    #   3. Add [buf didModifyRange:] after CPU writes (required for Managed mode).\n    #\n    # We use Python for portable in-place sed-style replacement so we don't\n    # depend on GNU sed vs BSD sed quirks.\n    python3 - \"$src\" <<'PYEOF'\nimport sys, re\n\npath = sys.argv[1]\nwith open(path, 'r') as f:\n    text = f.read()\n\n# ── PATCH 1 ──────────────────────────────────────────────────────────────────\n# Insert a helper function after the first #include block / before @implementation\nHELPER = \"\"\"\n// TrashClaw discrete-GPU patch (ref: llama.cpp PR #20615)\n// Returns MTLStorageModeManaged on discrete GPUs (non-UMA), MTLStorageModeShared\n// on Apple Silicon / integrated GPUs with unified memory.\nstatic MTLStorageMode ggml_metal_storage_mode(id<MTLDevice> device) {\n    if (@available(macOS 10.15, *)) {\n        // hasUnifiedMemory is the correct API on 10.15+\n        if (device.hasUnifiedMemory) {\n            return MTLStorageModeShared;\n        }\n    } else {\n        // Fallback: low-power devices are integrated / UMA\n        if (device.isLowPower) {\n            return MTLStorageModeShared;\n        }\n    }\n    return MTLStorageModeManaged;\n}\n\"\"\"\n\n# Insert before the first @implementation line\nif 'ggml_metal_storage_mode' not in text:\n    text = re.sub(\n        r'(@implementation\\s+\\w)',\n        HELPER + r'\\1',\n        text,\n        count=1\n    )\n\n# ── PATCH 2 ──────────────────────────────────────────────────────────────────\n# Replace `storageMode:MTLStorageModeShared` with our helper.\n# Only within buffer allocation calls (lines containing newBufferWithLength or\n# newBufferWithBytes) to avoid touching unrelated code.\ndef replace_storage_in_alloc(m):\n    return m.group(0).replace(\n        'MTLStorageModeShared',\n        'ggml_metal_storage_mode(ctx->device)'\n    )\n\ntext = re.sub(\n    r'(newBuffer(?:WithLength|WithBytes)[^;]+?storageMode\\s*:\\s*)MTLStorageModeShared',\n    lambda m: m.group(1) + 'ggml_metal_storage_mode(ctx->device)',\n    text\n)\n\n# ── PATCH 3 ──────────────────────────────────────────────────────────────────\n# After any memcpy/memset into a buffer that was allocated with Managed storage,\n# we must call [buf didModifyRange:NSMakeRange(0, buf.length)] so Metal flushes\n# CPU cache to GPU-visible memory.  We instrument the known upload path.\n# Pattern: lines that call memcpy(buf.contents, ...) followed by no didModifyRange\nDID_MODIFY = (\n    '    if (buf.storageMode == MTLStorageModeManaged) {\\n'\n    '        [buf didModifyRange:NSMakeRange(0, buf.length)];\\n'\n    '    }\\n'\n)\ntext = re.sub(\n    r'(memcpy\\(([a-zA-Z_][a-zA-Z0-9_]*)\\s*\\.contents,[^;]+;\\n)(?!\\s*\\[\\2\\s+didModifyRange)',\n    lambda m: m.group(1) + DID_MODIFY.replace('buf', m.group(2)),\n    text\n)\n\nwith open(path, 'w') as f:\n    f.write(text)\nprint('[PATCH] StorageModeManaged patch written successfully.')\nPYEOF\n\n    print_ok \"Patch applied.\"\n}\n\n# ── cmake build ────────────────────────────────────────────────────────────────\nbuild_llama() {\n    local build_type=\"${1:-Release}\"\n    local cmake_build=\"$BUILD_DIR/llama.cpp/build\"\n    mkdir -p \"$cmake_build\" \"$INSTALL_DIR\"\n\n    print_info \"Configuring CMake (build_type=$build_type)...\"\n    cmake -S \"$BUILD_DIR/llama.cpp\" \\\n          -B \"$cmake_build\" \\\n          -DCMAKE_BUILD_TYPE=\"$build_type\" \\\n          -DGGML_METAL=ON \\\n          -DGGML_METAL_EMBED_LIBRARY=ON \\\n          -DCMAKE_INSTALL_PREFIX=\"$INSTALL_DIR\" \\\n          -DCMAKE_OSX_DEPLOYMENT_TARGET=10.13 \\\n          -DLLAMA_CURL=OFF \\\n          -Wno-dev \\\n          2>&1 | grep -E '(CMake Warning|CMake Error|-- Build|-- Found|-- Metal)'\n\n    local ncpu\n    ncpu=$(sysctl -n hw.logicalcpu 2>/dev/null || echo 4)\n    print_info \"Building with $ncpu parallel jobs...\"\n    cmake --build \"$cmake_build\" --config \"$build_type\" -j\"$ncpu\" 2>&1 | \\\n        grep -E '(error:|warning:.*error|Linking|Built target llama|Built target ggml)' || true\n\n    print_info \"Installing to $INSTALL_DIR...\"\n    cmake --install \"$cmake_build\" --config \"$build_type\" 2>&1 | tail -10\n    print_ok \"Build complete. Binaries in $INSTALL_DIR/bin/\"\n}\n\n# ── smoke test ─────────────────────────────────────────────────────────────────\nsmoke_test() {\n    local server=\"$INSTALL_DIR/bin/llama-server\"\n    [[ -x \"$server\" ]] || server=\"$INSTALL_DIR/bin/server\"\n    if [[ ! -x \"$server\" ]]; then\n        print_warn \"llama-server binary not found at expected path; skipping smoke test.\"\n        return\n    fi\n    print_info \"Verifying Metal in binary...\"\n    if \"