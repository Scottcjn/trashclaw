```json
{
  "solution_code": "=== FILE: build_llamacpp_metal.sh ===\n#!/bin/bash\n# TrashClaw: Patched llama.cpp build script for discrete AMD GPU support on macOS\n# Applies the StorageModeManaged fix from llama.cpp PR #20615\n# Tested hardware: Mac Pro 2013, iMac 2014-2019, MacBook Pro 2015-2019\n\nset -e\n\nLLAMA_DIR=\"${LLAMA_DIR:-./llama.cpp}\"\nLLAMA_REPO=\"https://github.com/ggml-org/llama.cpp.git\"\nLLAMA_TAG=\"${LLAMA_TAG:-b5616}\"  # Pin to a known-good tag; override with LLAMA_TAG env var\nINSTALL_DIR=\"${INSTALL_DIR:-./llama_bin}\"\n\necho \"[TrashClaw] Building llama.cpp with discrete AMD GPU (Metal StorageModeManaged) patch\"\necho \"[TrashClaw] Target dir: $LLAMA_DIR\"\necho \"[TrashClaw] Install dir: $INSTALL_DIR\"\n\n# ── 1. Clone or update ──────────────────────────────────────────────────────\nif [ ! -d \"$LLAMA_DIR/.git\" ]; then\n    echo \"[TrashClaw] Cloning llama.cpp ...\"\n    git clone --depth 1 --branch \"$LLAMA_TAG\" \"$LLAMA_REPO\" \"$LLAMA_DIR\"\nelse\n    echo \"[TrashClaw] llama.cpp already cloned at $LLAMA_DIR — skipping clone.\"\nfi\n\ncd \"$LLAMA_DIR\"\n\n# ── 2. Apply the StorageModeManaged patch (PR #20615) ───────────────────────\n# The 3-line fix changes MTLStorageModeShared → MTLStorageModeManaged so that\n# buffers are visible to discrete AMD GPUs that lack unified memory.\nPATCH_TARGET=\"ggml/src/ggml-metal/ggml-metal.m\"\n\nif [ ! -f \"$PATCH_TARGET\" ]; then\n    echo \"[TrashClaw] ERROR: Cannot find $PATCH_TARGET — wrong llama.cpp version?\"\n    exit 1\nfi\n\nif grep -q 'StorageModeManaged.*TRASHCLAW_PATCH' \"$PATCH_TARGET\" 2>/dev/null; then\n    echo \"[TrashClaw] Patch already applied — skipping.\"\nelse\n    echo \"[TrashClaw] Applying StorageModeManaged patch ...\"\n    # The exact patch from PR #20615:\n    # In ggml_metal_init, when allocating host-visible buffers for non-unified\n    # (discrete) GPUs, use MTLStorageModeManaged instead of MTLStorageModeShared.\n    #\n    # Original lines (may vary slightly across minor versions):\n    #   MTLResourceStorageModeShared\n    # Replacement:\n    #   (ctx->device.hasUnifiedMemory ? MTLResourceStorageModeShared : MTLResourceStorageModeManaged)\n    #\n    # We use Python for the replacement so we don't need GNU sed on macOS.\n    python3 - <<'PYEOF'\nimport re, sys\n\npath = \"ggml/src/ggml-metal/ggml-metal.m\"\nwith open(path, \"r\") as f:\n    src = f.read()\n\n# Patch 1: buffer allocation storage mode (main fix)\n# Replace bare MTLStorageModeShared in newBufferWithLength calls with\n# a conditional that picks Managed for discrete GPUs.\npattern = r'(newBufferWithLength:[^\\n]+options:)[^\\n]*(MTLResourceStorageModeShared)([^\\n]*)'\nreplacement = (\n    r'\\1'\n    r'(ctx->device.hasUnifiedMemory ? MTLResourceStorageModeShared : MTLResourceStorageModeManaged) /* TRASHCLAW_PATCH */'\n    r'\\3'\n)\nnew_src, count = re.subn(pattern, replacement, src)\n\nif count == 0:\n    # Fallback: broader search for the specific storage mode constant in buffer alloc context\n    pattern2 = r'(MTLResourceOptions)(\\s*=\\s*)(MTLResourceStorageModeShared)'\n    new_src, count = re.subn(pattern2,\n        r'\\1\\2(ctx->device.hasUnifiedMemory ? MTLResourceStorageModeShared : MTLResourceStorageModeManaged) /* TRASHCLAW_PATCH */',\n        src)\n\nif count == 0:\n    print(\"[TrashClaw] WARNING: Could not locate patch site in \" + path)\n    print(\"[TrashClaw] Manual review required. Continuing with unpatched build.\")\n    sys.exit(0)\n\nwith open(path, \"w\") as f:\n    f.write(new_src)\n\nprint(f\"[TrashClaw] Patched {count} occurrence(s) in {path}\")\nPYEOF\nfi\n\n# ── 3. Build ─────────────────────────────────────────────────────────────────\necho \"[TrashClaw] Building with Metal support ...\"\nmkdir -p build\ncd build\n\ncmake .. \\\n    -DGGML_METAL=ON \\\n    -DGGML_METAL_EMBED_LIBRARY=ON \\\n    -DCMAKE_BUILD_TYPE=Release \\\n    -DCMAKE_INSTALL_PREFIX=\"../../$INSTALL_DIR\"\n\ncmake --build . --config Release -j\"$(sysctl -n hw.logicalcpu)\"\ncmake --install .\n\ncd ../..\n\necho \"[TrashClaw] Build complete. Binaries in: $INSTALL_DIR/bin/\"\necho \"[TrashClaw] Run: $INSTALL_DIR/bin/llama-server --help\"\n\n\n=== FILE: metal_gpu_detect.py ===\n\"\"\"\nTrashClaw Metal GPU detection module.\nDetects discrete vs unified GPU on macOS and reports Metal status.\nWorks with Python 3.7+ stdlib only (no dependencies).\n\"\"\"\n\nimport subprocess\nimport platform\nimport os\nimport json\nfrom typing import Dict, Any\n\n\ndef _run(cmd: list) -> str:\n    \"\"\"Run a shell command and return stdout, empty string on failure.\"\"\"\n    try:\n        result = subprocess.run(\n            cmd,\n            capture_output=True,\n            text=True,\n            timeout=10\n        )\n        return result.stdout.strip()\n    except Exception:\n        return \"\"\n\n\ndef detect_metal_gpu() -> Dict[str, Any]:\n    \"\"\"\n    Detect Metal GPU capabilities on macOS.\n\n    Returns a dict with keys:\n      platform        : 'macos' | 'other'\n      metal_supported : bool\n      gpu_type        : 'unified' | 'discrete' | 'unknown'\n      gpu_name        : str\n      has_unified_memory: bool\n      metal_patched_build: bool  — True if llama_bin built with TrashClaw patch\n      status_line     : str      — human-readable one-liner\n      warnings        : list[str]\n    \"\"\"\n    info: Dict[str, Any] = {\n        \"platform\": \"other\",\n        \"metal_supported\": False,\n        \"gpu_type\": \"unknown\",\n        \"gpu_name\": \"unknown\",\n        \"has_unified_memory\": False,\n        \"metal_patched_build\": False,\n        \"status_line\": \"Metal: N/A (not macOS)\",\n        \"warnings\": [],\n    }\n\n    if platform.system() != \"Darwin\":\n        return info\n\n    info[\"platform\"] = \"macos\"\n\n    # ── macOS version ─────────────────────────────────────────────────────────\n    macos_ver = platform.mac_ver()[0]  # e.g. '14.4.1'\n    info[\"macos_version\"] = macos_ver\n\n    # ── system_profiler GPU info ───────────────────────────────────────────────\n    sp_json = _run([\"system_profiler\", \"SPDisplaysDataType\", \"-json\"])\n    gpu_name = \"unknown\"\n    has_unified = False\n\n    if sp_json:\n        try:\n            data = json.loads(sp_json)\n            displays = data.get(\"SPDisplaysDataType\", [])\n            if displays:\n                gpu = displays[0]  # primary GPU\n                gpu_name = gpu.get(\"sppci_model\", gpu.get(\"_name\", \"unknown\"))\n                # Apple Silicon reports 'sppci_bus' as 'spi'\n                bus = gpu.get(\"sppci_bus\", \"\").lower()\n                # 'built-in' or 'spi' → unified; 'pcie' → discrete\n                if \"spi\" in bus or \"built-in\" in bus:\n                    has_unified = True\n                elif \"pcie\" in bus or \"pci\" in bus:\n                    has_unified = False\n                else:\n                    # Heuristic: Apple Silicon names contain 'Apple'\n                    has_unified = \"apple\" in gpu_name.lower()\n        except (json.JSONDecodeError, KeyError, IndexError):\n            info[\"warnings\"].append(\"Could not parse system_profiler JSON\")\n\n    # Fallback: ioreg for hasUnifiedMemory\n    if gpu_name == \"unknown\" or not sp_json:\n        ioreg_out = _run([\"ioreg\", \"-l\", \"-w\", \"0\"])\n        if \"hasUnifiedMemory\" in ioreg_out:\n            has_unified = \"Yes\" in ioreg_out[ioreg_out.find(\"hasUnifiedMemory\"):ioreg_out.find(\"hasUnifiedMemory\")+40]\n        # Try to get GPU name from ioreg\n        for line in ioreg_out.splitlines():\n            if \"model\" in line.lower() and (\"amd\" in line.lower() or \"nvidia\" in line.lower() or \"intel\" in line.lower() or \"apple\" in line.lower()):\n                # Extract quoted value\n                parts = line.split('\"')\n                if len(parts) >= 4:\n                    gpu_name = parts[3]\n                    break\n\n    info[\"gpu_name\"] = gpu_name\n    info[\"has_unified_memory\"] = has_unified\n    info[\"gpu_type\"] = \"unified\" if has_unified else \"discrete\"\n\n    # ── Metal support: Metal exists on macOS 10.11+ ───────────────────────────\n    try:\n        major = int(macos_ver.split(\".\")[0]) if macos_ver else 0\n        minor = int(macos_ver.split(\".\")[1]) if macos_ver and len(macos_ver.split(\".\")) > 1 else 0\n        info[